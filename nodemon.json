{
  "ignore": [
    "node_modules",
    "schema.graphql",
    "schema.json",
    "**/*.test.ts",
    "**/*.spec.ts",
    ".git",
    "types/schema.d.ts"
  ],
  "watch": ["src", "types"],
  "env": {
    "NODE_ENV": "development"
  },
  "ext": "ts,js",
  "exec": "yarn tslint && ts-node --transpile-only src/startServer.ts"
}
